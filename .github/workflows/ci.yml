name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  docs:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: docs/requirements.txt

      - name: Install documentation dependencies
        run: python -m pip install --requirement docs/requirements.txt

      - name: Build documentation
        run: npm run docs:build

  validate:
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
    runs-on: ${{ matrix.os }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check package metadata
        run: npm run check:package-metadata

      - name: Check formatting
        run: npm run format:check

      - name: Run lint
        run: npm run lint

      - name: Build package
        run: npm run build

      - name: Run tests
        run: npm run test:ci

      # E2E tests are skipped in CI by default due to long execution time (5+ minutes)
      # To enable, uncomment the following step or run manually with: npm run test:e2e
      # - name: Run E2E tests
      #   run: npm run test:e2e

      - name: Verify npm package tarball
        run: npm pack ./dist/angular-django2 --dry-run

      - name: Verify tracked files are unchanged
        run: git diff --exit-code
