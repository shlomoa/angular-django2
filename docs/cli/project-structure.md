# project-structure

Create the standard `core/`, `shared/`, and `features/` structure.

```bash
ng generate angular-django2:project-structure --project=<name>
```

Options: `--prefix` default `app`.
