{
  "$schema": "http://json-schema.org/schema",
  "$id": "AngularDjango2Compile",
  "title": "angular-django2 compile schematic",
  "description": "Compile a complete Angular Material application from one OpenUI document.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "document": {
      "type": "string",
      "format": "path",
      "description": "Workspace-relative path to a canonical OpenUI JSON document (for example app.openui.json) with one Application node.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["document"]
}
